
## 环境与位置（2026-09-14 迁移）
- 本项目已从 iCloud 同步的 ~/Documents 迁出，现位于 Mac mini 的 `~/01 Ongoing AI projects/` 下，**不再被 iCloud 同步**。
- 备份与多机同步只靠 git：开工先 `git pull`，收工必 `git push`（远端见 `git remote -v`；LEK project 无远端，仅本地）。
- 数据、缓存、模型权重由 .gitignore 挡在仓库外，只存在于 mini 本地；在其他机器上需重算或重新下载。
- 不要把任何代码项目移回 ~/Documents 或 iCloud 云盘。
